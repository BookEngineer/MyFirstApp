<!DOCTYPE html>
<html>
<head>
  <title>Bookhistory</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" rel="stylesheet">
  <%= javascript_importmap_tags %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <img src="/assets/logo.jpg" alt="ロゴ" width="30" height="24" class="d-inline-block align-text-top">
        BookHistory
      </a>
      <a class="navbar-brand" href="/">
        <i class="bi bi-house"></i>
      </a>
       <% if user_signed_in? %>
          <span class="navbar-text">
            <%= current_user.name %>でログイン中
          </span>
          <%= link_to "マイページ", mypage_user_path(current_user), class: "btn btn-outline-primary ms-2" %>
          <%= link_to "ログアウト", destroy_user_session_path, method: :get, class: "btn btn-outline-danger ms-2" %>
        <% else %>
          <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-success" %>
        <% end %>
        <%= link_to "本を探す", search_books_search_path, class: "btn btn-outline-info ms-auto" %>
      </div>
      
    </div>
  </nav>

  <div class="container my-4">
    <%= yield %>
  </div>

  <footer class="bg-light text-center py-3">
    <p>&copy; <%= Time.now.year %> Bookhistory. All rights reserved.</p>
  </footer>
</body>
</html>
